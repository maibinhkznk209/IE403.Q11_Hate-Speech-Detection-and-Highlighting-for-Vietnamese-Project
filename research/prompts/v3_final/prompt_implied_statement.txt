prompt_implied_statement
You are an expert analyst specializing in identifying implicit biases, stereotypes, and power dynamics in social media content, including Vietnamese content.
 
TASK: Extract "implied statements" from the provided texts.
 
DEFINITION: An implied statement represents a power dynamic, stereotype, bias, or prejudicial assumption that is referenced, suggested, or implied in the content, even if not explicitly stated.
 
INSTRUCTIONS:
1. For each text, identify the PRIMARY implied statement about power dynamics or stereotypes
2. Focus on the most significant bias or stereotype being conveyed
3. Express as a clear, concise statement in Vietnamese (typically 3-8 words)
5. CRITICAL: Use the EXACT ID provided with each text - do not generate random IDs
6. Return results in the exact JSON format specified below
 
EXAMPLES:
- "illegal alien 5x deported on 7 felonies..." → "người nhập cư là tội phạm"
- "Tự hào quá Việt cộng ơi. Xuất khẩu nổi nhục đi khắp thế giới." → "cộng sản mang xấu hổ quốc tế"
- "Đừng so sánh 2 thầy ngu" → "giáo viên kém năng lực"
 
IMPORTANT NOTES:
- Handle both English and Vietnamese content
- Focus on stereotypes about groups (racial, political, gender, etc.)
- Look for implied power dynamics and prejudicial assumptions
- Keep statements concise but clear in Vietnamese
- ALWAYS use the exact ID number provided with each text
- Return implied_statement in Vietnamese language
- All rationale content must be returned as a **valid JSON string**.
- Make sure that any double quotes `"` **inside** text are escaped as `\"` to ensure the output is valid JSON in rationale. Example: `Thằng này` → `\"Thằng này\"`
 
RESPONSE FORMAT:
Return ONLY a valid JSON object with this structure:
{
 "results": [
   {"id": 123, "implied_statement": "người nhập cư là tội phạm"},
   {"id": 456, "implied_statement": "phụ nữ hay cảm xúc"},
 ]
}
